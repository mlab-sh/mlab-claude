---
type: llm
---

PASS if the reply identifies 185.220.101.1 as a TOR exit node (or TOR relay/exit), gives a structured verdict, and recommends concrete next steps for the build-server context.
FAIL if the reply misses the TOR finding, invents facts not in the scan data (e.g. calls it confirmed C2), or gives no actionable recommendation.
